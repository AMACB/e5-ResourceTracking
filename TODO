TODO

POSSIBLE BUGS - modifying the num_available by admin OR admin approving a checkout OR removing the item entirely needs to notify users too many of the item in their cart
	(if item is destroyed, notify users with it in cart)
	// if the cart is already submitted, it needs to "unsubmit it" and notify the user
	

make the items#show not look ugly
add tooltips for description
add images (use thumbnails in items#index)

contact us form/page
homepage full

search/sort through items (watch out when AJAX adding)

when adding to cart, make "View Cart (x Items)" flash red or something

when My Cart removed to empty, refresh the page?

**in item manager: AJAX the contents of "Edit" when needed; clear the form when closed; close the form when submit
	in edit form: add spacing, labels; show errors

optimize the "Already added" error detection??

user email verification; check for city email?

ADD NOTIFICATIONS FOR USERS (can be viewed in "profile" or something)
email notifications?

BUG:
	when items#update fails (in the else block), need error message

verification of dates both client side and server side during checkout
admin approve checkouts
when approved, notify users who also have a **pending checkout** that contained a checked-out item that is no longer available
view status of checkout

moderation tools - disable user from submitting stuff, etc.

TESTING
go through stuff as non-user, user, and admin

FEATURES (optional)
reCAPTCHA integration
