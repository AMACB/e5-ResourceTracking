# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  notifications:
    types:
      request_invalidated: "Request was rejected automatically"
      request_rejected: "Request was rejected by admin"
      request_approved: "Request was approved"
  requests:
    date_begin: "Date range (begin)"
    date_end: "Date range (end)"
  info:
    email: "PARDLendingLibrary@austintexas.gov"
    hours: "Tuesday and Thursday from 10:30am to 1:30pm"
    address: "2209 Rosewood Avenue, 211, Austin, TX 78702"
#    legal:
#      request: "Undersigned agrees that the equipment shall be returned to CPD – Dewitty office by date provided above. The undersigned also agrees that if item(s) are returned damaged, dirty, late, wet, moldy or missing parts while in use; replacement, cleaning fees or repair of item will be charged to facility FDU. More than 3 violations will result in being suspended from reserving Lending Library Commodities for 3 months."